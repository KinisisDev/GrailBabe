{
  "name": "grailbabe",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "type-check": "pnpm -r run typecheck",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
